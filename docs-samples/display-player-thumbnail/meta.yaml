"description": |
  This code sample displays the current player's thumbnail in a parent
  `ImageLabel` by using `Class.Players:GetUserThumbnailAsync()` and setting the
  `Class.ImageLabel.Image|Image()` property as well as its
  `Class.GuiObject.Size|Size()`.
"display_name": |-
  Display Player Thumbnail
"expected_output": |-
  When run, the parent `ImageLabel` should have its `Class.ImageLabel.Image|Image()` and `Class.GuiObject.Size|Size()` set accordingly.
"highlight_lines": ""
"category": Docs Sample
