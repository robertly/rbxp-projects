local BASE_URL = "http://www.roblox.com/asset/?id=%d"
game:Load(BASE_URL:format(1818)) -- Loads Crossroads, an original, *uncopylocked* game by Roblox

-- NOTE:  If trying to access a copylocked placed that is not owned by the currently logged in user,
-- or if used in any code with a security context less than 4, this will not work, and will error.
