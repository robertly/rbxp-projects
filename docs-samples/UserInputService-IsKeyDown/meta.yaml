"description": |
  This example uses the `Class.UserInputService:IsKeyDown()` function to create
  different behaviors when a shift key is held down than when a shift key is not
  held down when user input `UserInputBegan|begins`.

  The local function _IsShiftKeyDown()_ returns whether the left or right shift
  `Enum.KeyCode|keys` are pressed.
"display_name": |-
  Special Action on Key Combo Press
"expected_output": |-
  Output may vary depending on whether Space key is pressed by the user during testing.
"highlight_lines": ""
"category": Docs Sample
