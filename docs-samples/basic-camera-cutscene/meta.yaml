"description": |
  In this code sample a simple `Camera` animation is played after the
  `Class.Players.LocalPlayer` spawns using `TweenService`. The
  `Class.Camera.CameraType` of the `Camera` is set to _'Scriptable'_ so the
  default camera scripts do not interfere.
"display_name": |-
  Basic Camera Cutscene
"expected_output": ""
"highlight_lines": ""
"category": Docs Sample
