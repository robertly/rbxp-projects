"description": |
  The following example creates an `awardBadge()` function that handles
  potential errors that may occur when awarding a badge. Using properties of the
  badge fetched via `Class.BadgeService:GetBadgeInfoAsync()`, it confirms that
  the badge can be awarded and does so using `Class.BadgeService:AwardBadge()`.
"display_name": |-
  Awarding a Badge
"expected_output": ""
"highlight_lines": |
  14
"category": Docs Sample
