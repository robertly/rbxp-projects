--!strict

--[[
	Contains constants only used within Farm scripts
--]]

local Workspace = game:GetService("Workspace")

local FarmConstants = {
	FarmContainer = Workspace.Farms,
	MaxInteractionDistance = 15,
	SeedPrefix = "Seed",
}

return FarmConstants
