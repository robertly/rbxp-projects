--[[
	Attribute names set on the GUIs

	'selectedIndex' is the index of the selected blaster for PickABlasterGui.
--]]

local GuiAttribute = {
	selectedIndex = "selectedIndex",
}

return GuiAttribute
